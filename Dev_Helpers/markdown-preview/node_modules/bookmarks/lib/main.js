(function() {
  var Bookmarks, BookmarksView;

  Bookmarks = null;

  BookmarksView = null;

  module.exports = {
    activate: function() {
      var bookmarksView;
      bookmarksView = null;
      atom.workspaceView.command('bookmarks:view-all', function() {
        if (typeof bookmarksList === "undefined" || bookmarksList === null) {
          if (BookmarksView == null) {
            BookmarksView = require('./bookmarks-view');
          }
          bookmarksView = new BookmarksView();
        }
        return bookmarksView.toggle();
      });
      return atom.workspaceView.eachEditorView(function(editor) {
        if (editor.attached && (editor.getPane() != null)) {
          if (Bookmarks == null) {
            Bookmarks = require('./bookmarks');
          }
          return new Bookmarks(editor);
        }
      });
    }
  };

}).call(this);

//# sourceMappingURL=/../../../../../../../../..//tmp/atom-build/Atom.app/Contents/Resources/app/node_modules/bookmarks/lib/main.js.map
