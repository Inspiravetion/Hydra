(function() {
  var Reporter;

  Reporter = null;

  module.exports = {
    activate: function() {
      if (!atom.config.get('exception-reporting.userId')) {
        atom.config.set('exception-reporting.userId', require('guid').raw());
      }
      return this.uncaughtErrorSubscription = atom.on('uncaught-error', function(message, url, line, column, error) {
        if (Reporter == null) {
          Reporter = require('./reporter');
        }
        return Reporter.send(message, url, line, column, error);
      });
    },
    deactivate: function() {
      var _ref;
      if ((_ref = this.uncaughtErrorSubscription) != null) {
        _ref.off();
      }
      return this.uncaughtErrorSubscription = null;
    }
  };

}).call(this);

//# sourceMappingURL=/../../../../../../../../..//tmp/atom-build/Atom.app/Contents/Resources/app/node_modules/exception-reporting/lib/exception-reporting.js.map
